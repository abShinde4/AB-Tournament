import "./squad-team.css";

const SquadTeamLockedBanner = () => (
  <div className="squad-locked-banner">🔒 Team locked — tournament has started. No edits or roster changes allowed.</div>
);

export default SquadTeamLockedBanner;
